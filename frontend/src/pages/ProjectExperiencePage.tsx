import * as React from "react";

import Box from "@mui/material/Box";

import '../utils/font-format.scss';

export const ProjectExperiencePage = (): JSX.Element => {
  return (
    <Box>

    </Box>
  );
}
